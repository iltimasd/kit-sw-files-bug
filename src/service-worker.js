import { build, files, timestamp } from "$service-worker";

//--  uncomment 2 lines below, and comment line above
// import { build, assets, timestamp } from "$service-worker";
// const files = assets;

console.log(files);
